import logo from '../assets/images/logo.png';

const Footer = () => {
    return (
        <footer>
            <img src={logo} alt="company-logo"></img>
        </footer>
    );
};

export default Footer;